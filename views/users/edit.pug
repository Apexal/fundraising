extends /layout

block content
    .row
        .col-xs-12
            .page-header
                h1 Edit #{targetUser.name.full}
    .row
        .col-xs-12
            form.form
                

            a.btn.btn-danger(href="#") Unverify #{targetUser.name.first}