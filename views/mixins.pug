mixin icon(name, text)
    i.fa(class="fa-" + name, aria-hidden="true")
    if text
        |  #{text}

mixin help(label)
    i.fa.fa-question-circle.help-icon(onclick="alert('" + helpers.getHelpInfo(label) + "');", aria-hidden="true", title=helpers.getHelpInfo(label))