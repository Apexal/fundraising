nav.navbar.navbar-inverse.navbar-fixed-top.navbar-top
    .container
        .navbar-header
            a.navbar-brand(href="#")
                img(alt="Kids Tales", src="/images/small-logo.gif", style="height: 100%")
            button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
                span.icon-bar
                span.icon-bar
                span.icon-bar
            a.navbar-brand(href='/') Kids Tales 
                if loggedIn
                    span.badge= user.region.name
        #navbar.collapse.navbar-collapse
            ul.nav.navbar-nav
                if loggedIn
                    include servicedropdowns
            include navbarright