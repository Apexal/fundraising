ul.nav.navbar-nav.navbar-right
    if loggedIn
        // Logged in indicator with user actions
        if user.verified
            li.hidden-xs
                a.user-image-link(href="/profile")
                    img.user-image(src="/images/" + user.profileImageName)
        li.dropdown
            a.dropdown-toggle(href='#', data-toggle='dropdown', role='button', aria-haspopup='true', aria-expanded='false')
                if user.admin
                    b Admin
                else if !user.verified
                    b Unverified
                else
                    b= user.rankName
                |  #{user.name.full}
                span.caret
            ul.dropdown-menu
                if user.verified
                    li(title="Edit your profile.")
                        a(href='/profile') Edit Profile
                    li(title="View your public profile.")
                        a(href='/users/' + user.email) View Profile
                    li.divider(role='separator')
                li
                    a(href='/logout') Logout
    else
        li
            a(href='/login') Login